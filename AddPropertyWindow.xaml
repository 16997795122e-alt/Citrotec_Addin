<Window x:Class="AddPropertyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Adicionar Nova Propriedade" Height="300" Width="450"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent"
        WindowStartupLocation="CenterOwner" Topmost="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Estilos.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Background="White" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Adicionar Nova Propriedade" Grid.Row="0" FontWeight="SemiBold" FontSize="16" Foreground="#333" Margin="20"/>

            <StackPanel Grid.Row="1" Margin="20,0">
                <Label Content="Nome da Propriedade (Interno, sem espaços ou acentos)" Foreground="#6E7687" Margin="0,0,0,5"/>
                <TextBox x:Name="InternalNameTextBox" Padding="10,8" BorderThickness="1" BorderBrush="#E0E5F0"/>

                <Label Content="Nome de Exibição (Rótulo que aparecerá na tela)" Foreground="#6E7687" Margin="0,15,0,5"/>
                <TextBox x:Name="DisplayNameTextBox" Padding="10,8" BorderThickness="1" BorderBrush="#E0E5F0"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
                <Button x:Name="btnCancel" Content="Cancelar" IsCancel="True" Click="btnCancel_Click" Style="{StaticResource AppleButtonStyle}"/>
                <Button x:Name="btnOK" Content="OK" IsDefault="True" Click="btnOK_Click" Style="{StaticResource AppleButtonStyle}" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>