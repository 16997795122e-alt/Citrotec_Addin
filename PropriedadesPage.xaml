<Page x:Class="PropriedadesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="PropriedadesPage"
      Background="Transparent">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Estilos.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <ContextMenu x:Key="TextBoxContextMenu">
                <MenuItem Command="ApplicationCommands.Cut" Header="Recortar"/>
                <MenuItem Command="ApplicationCommands.Copy" Header="Copiar"/>
                <MenuItem Command="ApplicationCommands.Paste" Header="Colar"/>
            </ContextMenu>

            <Style x:Key="SectionHeaderStyle" TargetType="Label">
                <Setter Property="Foreground" Value="#1C202A"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Margin" Value="0,15,0,15"/>
            </Style>

            <Style x:Key="ModernTextBox" TargetType="TextBox">
                <Setter Property="Padding" Value="10,8,80,8"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="#E0E5F0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="ContextMenu" Value="{StaticResource TextBoxContextMenu}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border" CornerRadius="6" Background="White" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#faa61a"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="Label" x:Key="PropertyLabelStyle">
                <Setter Property="Foreground" Value="#6E7687"/>
                <Setter Property="Margin" Value="0,0,10,5"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <DataTemplate x:Key="PropertyItemTemplate">
                <Grid Margin="0,0,25,18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Content="{Binding DisplayName}" Style="{StaticResource PropertyLabelStyle}"/>

                    <Grid Grid.Row="1">
                        <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" PreviewKeyDown="TextBox_PreviewKeyDown"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <Button x:Name="CalendarButton" Content="&#xE787;" FontFamily="Segoe MDL2 Assets" 
                                    Width="35" Height="35" FontSize="16" Background="Transparent" 
                                    BorderThickness="0" Foreground="#A0A0A0" Cursor="Hand" 
                                    ToolTip="Selecionar Data" 
                                    Click="CalendarButton_Click" IsTabStop="False" Focusable="False">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Type}" Value="Date">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <Button x:Name="DeleteButton" Content="&#xE74D;" FontFamily="Segoe MDL2 Assets" 
                                    Width="35" Height="35" FontSize="16"  
                                    ToolTip="Excluir Propriedade Customizada" 
                                    CommandParameter="{Binding}" Click="DeletePropertyButton_Click" IsTabStop="False" Focusable="False">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="Foreground" Value="#D32F2F"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCustom}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="#E57373"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </DataTemplate>

        </ResourceDictionary>
    </Page.Resources>

    <Border Padding="30,25">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Propriedades do Documento" FontSize="28" FontWeight="Bold" Foreground="#1C202A" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,15">
                <Button x:Name="ApplyButton" Content="Aplicar Alterações" Style="{StaticResource AppleButtonStyle}" Click="ApplyButton_Click" IsEnabled="False" Margin="10,0,0,0" IsTabStop="False"/>
                <Button x:Name="SyncSectraButton" Content="Sincronizar Sectra" Style="{StaticResource AppleButtonStyle}" Click="SyncSectraButton_Click" Margin="10,0,0,0" Visibility="Collapsed" IsTabStop="False"/>
            </StackPanel>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel MaxWidth="800" HorizontalAlignment="Left">

                    <Label x:Name="SectionHeaderLabel" Content="PROPRIEDADES DE DESENHO" Style="{StaticResource SectionHeaderStyle}"/>
                    <TextBlock x:Name="NoPropertiesMessage" Text="Nenhuma propriedade definida para este tipo de documento." Visibility="Collapsed" Foreground="#888" FontStyle="Italic"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <ItemsControl x:Name="LeftItemsControl" Grid.Column="0" ItemTemplate="{StaticResource PropertyItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>

                        <ItemsControl x:Name="RightItemsControl" Grid.Column="1" ItemTemplate="{StaticResource PropertyItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </Grid>

                    <Button x:Name="AddPropertyButton" Content="+ Adicionar Nova Propriedade" Style="{StaticResource AppleButtonStyle}" HorizontalAlignment="Left" Click="AddPropertyButton_Click" Margin="2,10,0,0" IsTabStop="False"/>
                </StackPanel>
            </ScrollViewer>

            <Border x:Name="NotificationBorder" VerticalAlignment="Bottom" HorizontalAlignment="Center" Height="40" Margin="20" Padding="30,0" CornerRadius="20" Visibility="Collapsed">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" Color="#E0E5F0" Opacity="0.6" BlurRadius="15"/>
                </Border.Effect>
                <TextBlock x:Name="NotificationTextBlock" Text="Mensagem de teste" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</Page>